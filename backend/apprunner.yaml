version: 1.0
runtime: python311
build:
  commands:
    pre-build:
      - yum install -y postgresql-devel
    build:
      - pip install -r requirements.txt
run:
  command: sh entrypoint.sh
  network:
    port: 8000
  env:
    - name: PYTHONPATH
      value: "."
